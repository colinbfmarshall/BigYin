<div class="container">
  <div class="row"> 
    <div class="col-sm-10 col-sm-offset-1">

      <div class="panel panel-default">
        <div class="panel-heading center">
          <h3>Current Topics</h3>
        </div>
        <div class="panel-body">
          <table class="table">
            <thead>
              <tr>
                <th><h5>#</h5></th>
                <th><h5>Title</h5></th>
                <th class="hidden-xs"><h5>Location</h5></th>
                <th><h5>Videos</h5></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% @topics.each_with_index do |topic, index| %>
                <%= link_to topic_posts_path(topic) do %>
                <tr>
                  <td><%= index + 1 %></td>
                  <td><%= topic.title %></td>
                  <td class="hidden-xs"><%= topic.location %></td>
                  <td><%= topic.posts.count %></td>
                  <td><%= link_to 'View', topic_posts_path(topic) %></td>
                </tr>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>

    </div>
  </div>

<br>
  <% if current_user.present? %>
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <%= link_to 'ADD NEW TOPIC', new_topic_path, class:'btn btn-flat flat-inverse btn-rounded btn-block' %>
    </div>
  </div>
<% end %>

</div>
